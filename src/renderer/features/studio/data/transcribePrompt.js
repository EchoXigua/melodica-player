export const TRANSCRIBE_PROMPT = `请把这张简谱图片转写成下面这种纯文本格式，我要直接粘贴进一个只认这种语法的播放器。严格按规则来，不确定的地方标注出来，不要瞎猜。

【无论如何都要给我可用的代码块，这是硬性要求】
- 不管原谱清不清楚、有没有标时值、你确定还是不确定，最后都必须输出至少一个完整的音符代码块，让我能直接复制粘贴使用——哪怕只是逐字直译、全按 1 拍处理，也比什么都不给强。
- 不确定的地方用 // 标注说明就行，不要因为拿不准就不输出代码块，或者只说明情况不给结果。

【音符写法】
- 中音 1-7（对应简谱不带点的 do re mi fa sol la ti），不要用其它符号。
- 低音在数字前加 <（简谱里音符下方一个点），例如 <1；高音在数字前加 >（音符上方一个点），例如 >1。只支持升/降一个八度，原谱里高两个点或低两个点的音，按听感就近折到本调内最靠近的八度，写清楚哪一句是这样处理的。
- 升号在数字前加 #（写在 < 或 > 后面，数字前面），例如 #4、<#2。原谱如果是降号（bXX），换算成等音的升号写法；换算不确定的写在文本末尾说明，不要直接编。
- 休止符固定写成 0，不能加 < > # 这几个符号。

【时长写法】
- 默认每个音是 1 拍（四分音符），时长不是 1 拍才需要写 :拍数，例如 1:0.5（八分音符）、1:2（二分音符）、1:1.5（附点四分音符）。
- 原谱里的横线延长（-）、下划线缩短（八分线一条 = 0.5 拍，十六分线两条 = 0.25 拍）、附点（在原时值上加一半），都要换算成一个 :拍数 数值合并写，不要保留 - _ . 这些符号。
- 拍数最小 1/16（0.0625），最大 32，超出范围的极端时值请拆成两个音符。
- 同一格子/小节里如果有连续相同音高的连音线（tie），合并成一个音符写总拍数；不支持连音符号本身。
- 除非原谱明确标了三连音记号（数字上方一条弧线+"3"），否则不要自己编出 0.333、0.667、0.1667 这类三分之一/六分之一的拍数——这种密集片段更可能是"音符+短休止"或普通的十六分音符被你看错了，先按 0.25/0.5 的整齐拍数处理，如果确实吃不准就在文末注明"这一句节奏不确定，按 XX 简化处理了"。

【休止符不要漏】
- 原谱上看到的换气记号、小节内的空白、明显的停顿，都要如实写成 0（可以带 :拍数），不要因为音符看起来挤在一起就假设没有休止而直接连成一长串音符。
- 如果一整段（比如超过 8 拍）完全没有任何休止符，转写完之后自己回头确认一遍：这段原谱是不是真的一口气不停顿，还是你漏看了某个短休止或换气记号。

【图片完全没有时值标记】
- 有些简谱图只有数字歌词对照（比如小红书那种歌词下面配数字），从头到尾没有横线、下划线、附点、任何节奏符号。这种情况下**照样要给出逐字转写的代码块**（每个音按 1 拍处理），只是在代码块外面另起一段说明一句"这张图没有标时值信息，以下是按每音 1 拍逐字转的"，不能因为没有时值标记就不给代码块。
- 如果你认得这首歌、确定自己知道它真实的旋律节奏，可以在逐字版代码块之外，额外提供第二个"按记忆补充节奏"的代码块，并在它前面明确写清楚"以下是我凭对原曲的印象补的节奏，不是照片上写的，请自行核对"。这一版是锦上添花，逐字版才是必须的。

【小节和排版】
- 音符之间用空格隔开；小节线可以写成 |，也可以不写，不影响播放，只是方便你自己对照原谱检查。
- 原谱一行对应输出一行，方便逐行核对，不强制要求。
- 只有确实不确定、需要我人工确认的地方，才用 // 后面写一句简短说明（比如 // 原谱模糊，按1处理），不要写标题、歌词、指法、力度记号、页码等其它内容。

【和弦/多声部】
- 本工具只支持单旋律。如果原谱是和弦或多声部（比如钢琴谱两行），只取主旋律/最高音那一行，并在最后用一句话说明你是怎么取的。

【输出要求】
- 正常情况下只输出一个代码块，代码块里只有音符文本（可以有 // 说明行），不要加任何标题、说明文字、markdown 装饰；只有前面说的"完全没有时值标记又认得这首歌"这种情况，才额外多给一个代码块。
- 代码块外面另起一段，报告：原谱上写没写速度（BPM/术语如 Andante）。如果写了就换算成 BPM 数字；如果没写，明确说"原谱未标注速度，需要自己试听调整"，不要编一个数字出来。
- 如果整体调号导致大量音符会落在 <1 以下或 >7 以上，建议直接说"建议把这段整体移低/移高一个八度再弹"，而不是逐个音符去折。

现在请转写我接下来发给你的这张简谱图片。`;
